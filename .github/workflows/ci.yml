name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: npm ci --workspaces --if-present
      - name: Run ESLint
        run: |
          if [ -f "backend/.eslintrc.cjs" ] || [ -f "backend/.eslintrc.js" ] || [ -f "backend/.eslintrc.json" ] || [ -f ".eslintrc.js" ] || [ -f ".eslintrc.json" ]; then
            npx eslint . --ext .js,.jsx --max-warnings 0
          else
            echo "No ESLint config found, skipping lint check"
          fi

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - name: Audit backend dependencies
        run: cd backend && npm audit --audit-level=high
      - name: Audit frontend dependencies
        run: |
          for app in admin teacher reception super-admin government; do
            echo "Auditing $app..."
            cd $GITHUB_WORKSPACE/$app && npm audit --audit-level=high
          done

  test-backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: uchqun_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    env:
      NODE_ENV: test
      DB_HOST: localhost
      DB_PORT: 5432
      DB_NAME: uchqun_test
      DB_USER: postgres
      DB_PASSWORD: postgres
      JWT_SECRET: ci-test-jwt-secret-that-is-at-least-32-chars-long
      JWT_REFRESH_SECRET: ci-test-jwt-refresh-secret-at-least-32-chars
      FRONTEND_URL: http://localhost:5173
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: cd backend && npm ci
      - name: Run backend tests
        run: cd backend && npm test

  test-frontend:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        app: [admin, teacher, reception, super-admin, government]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: cd ${{ matrix.app }} && npm ci
      - run: cd ${{ matrix.app }} && npm test -- --run

  build:
    runs-on: ubuntu-latest
    needs: [lint, security, test-backend, test-frontend]
    strategy:
      matrix:
        app: [admin, teacher, reception, super-admin, government]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
      - run: cd ${{ matrix.app }} && npm ci
      - run: cd ${{ matrix.app }} && npm run build
