// Removed per ADMIN-PORTAL-FOUNDATION — AI translation banner eliminated.
// File kept as stub so any stale imports compile without error.
const TranslationNotice = () => null;
export default TranslationNotice;
